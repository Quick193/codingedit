MONGO_URI=
JWT_SECRET=
# If unset, a development fallback secret is used automatically (set this for production)
PORT=5000
# Set to false to disable the in-memory MongoDB fallback when the primary connection fails
ENABLE_IN_MEMORY_DB=true
